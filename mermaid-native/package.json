{
  "name": "mermaid-native",
  "version": "1.0.0",
  "private": true,
  "description": "Forge app: renders Mermaid diagrams natively in Jira issue panels and a custom field.",
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix static/main-custom-ui",
    "postinstall": "npm install --prefix static/main-custom-ui"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.6.0"
  },
  "devDependencies": {
    "@forge/cli": "^13.0.0"
  }
}
